import { expect } from 'chai';

import reducer from '../../client/reducers/platformList';

import {
  LOGIN_PLATFORM,
  LOGOUT_PLATFORM,
  TOGGLE_MODAL,
  RECEIVE_SETTINGS_FIELDS,
} from '../../client/actions/platformListActions';

describe('Platform List Reducer', () => {

  it('should return initial state');

  it('should handle LOGIN_PLATFORM');

  it('should handle LOGOUT_PLATFORM');

  it('should handle TOGGLE_MODAL');

  it('should handle RECEIVE_SETTINGS_FIELDS');
});
