import React from 'react';
import { connect } from 'react-redux';
import { setSettingsFields } from '../actions/platformListActions'

let updateSettingsFields = ({ dispatch }) => {
  let input

  return (
    <div>
      <form onSubmit={}>

      </form>
    </div>
  )
}
updateSettingsFields = connect()(updateSettingsFields)

export default updateSettingsFields